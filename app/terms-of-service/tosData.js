// ToS tab structure. Content for each tab comes from the CMS (mapped by
// index in TermsClient.js's mergeWithCms). Bill Payment/Card Special/Send
// Abroad/WPS Service removed - the CMS only has one section (index 0), so
// those tabs had no content.
const tosData = [
  { tabName: "Consumer General" },
];

export default tosData;
